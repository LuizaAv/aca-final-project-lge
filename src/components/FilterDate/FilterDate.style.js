import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  select: {
    width: '20ch',
    height: 40,
    // padding: 10,
    // marginLeft: 5,
    // backgroundColor: '#fff',
  },

});
