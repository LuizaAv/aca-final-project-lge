import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  select: {
    borderRadius: 25,
    width: 130,
    height: 40,
    padding: 10,
    marginLeft: 5,
    backgroundColor: '#fff',
  },
  
});
