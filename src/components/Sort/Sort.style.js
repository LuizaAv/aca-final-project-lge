import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  select: {
    width: '20ch',
    height: 40,
    // marginLeft: 5,
    textAlign: 'center',
    // backgroundColor: '#fff',
  },
  menuItem: {
  },
});
