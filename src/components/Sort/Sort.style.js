import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  select: {
    borderRadius: 25,
    // width: 150,
    height: 40,
    marginLeft: 5,
    textAlign: 'center',
    backgroundColor: '#fff',
  },
  menuItem: {
  },
});
