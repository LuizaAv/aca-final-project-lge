import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(({
  iconButton: {
    marginRight: -20,
  },
  icon: {
    '&:hover': {
      transform: 'scale(1.2)',
    },
  },
}));
