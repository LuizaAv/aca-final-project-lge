import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  iconButton: {
    margin: -20,
    marginRight: 0,
  },
  icon: {
    '&:hover': {
      transform: 'scale(1.2)',
    },
  },
});
